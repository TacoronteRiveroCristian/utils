{
  "mcp.servers": {
    "influxdb": {
      "command": "node",
      "args": ["${workspaceFolder}/dist/index.js"],
      "env": {
        "INFLUX_HOST": "localhost",
        "INFLUX_PORT": "8086",
        "INFLUX_USERNAME": "admin",
        "INFLUX_PASSWORD": "admin",
        "LOG_LEVEL": "debug"
      }
    }
  }
}
